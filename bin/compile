#!/bin/sh

set -e

BUILD_DIR=$1
CACHE_DIR=$2

# config
echo "-----> Extract mediainfo"
echo $(pwd)
echo $(ls)

echo "-----> Exporting PATH and LIBRARY_PATH"
#PROFILE_PATH="$BUILD_DIR/.profile.d/mediainfo.sh"
#mkdir -p $(dirname $PROFILE_PATH)
#echo 'export PATH="$PATH:$HOME/vendor/mediainfo/usr/bin"' >> $PROFILE_PATH
#echo 'export LD_LIBRARY_PATH="$LD_LIBRARY_PATH:/app/vendor/libzen/usr/lib/x86_64-linux-gnu:/app/vendor/libmediainfo/usr/lib/x86_64-linux-gnu"' >> $PROFILE_PATH
